@page
@model RestauranteApp.Pages.Enderecos.IndexModel

@{
    ViewData["Title"] = "Endereços";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Meus Endereços</h2>
        <a asp-page="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Novo Endereço
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-hover mb-0 align-middle">
                    <thead class="table-secondary">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Perfil)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Titulo)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Logradouro)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Numero)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Complemento)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Bairro)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].Cidade)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].UF)</th>
                            <th>@Html.DisplayNameFor(model => model.Enderecos[0].CEP)</th>
                            <th class="text-center" style="width:220px;">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Enderecos)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Perfil.Id)</td>
                                <td>@Html.DisplayFor(modelItem => item.Titulo)</td>
                                <td>@Html.DisplayFor(modelItem => item.Logradouro)</td>
                                <td>@Html.DisplayFor(modelItem => item.Numero)</td>
                                <td>@Html.DisplayFor(modelItem => item.Complemento)</td>
                                <td>@Html.DisplayFor(modelItem => item.Bairro)</td>
                                <td>@Html.DisplayFor(modelItem => item.Cidade)</td>
                                <td>@Html.DisplayFor(modelItem => item.UF)</td>
                                <td>@Html.DisplayFor(modelItem => item.CEP)</td>
                                <td class="text-center">
                                    <div class="d-flex justify-content-center gap-2">
                                        <a asp-page="./Details" asp-route-id="@item.Id" class="btn btn-sm btn-info text-white">
                                            <i class="bi bi-eye"></i> Detalhes
                                        </a>
                                        <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning text-dark">
                                            <i class="bi bi-pencil"></i> Editar
                                        </a>
                                        <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                            <i class="bi bi-trash"></i> Excluir
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
